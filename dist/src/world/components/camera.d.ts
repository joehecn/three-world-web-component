import { PerspectiveCamera } from 'three';
export declare function createCamera(option?: [number, number, number, number], position?: [number, number, number], up?: [number, number, number], lookAt?: [number, number, number]): PerspectiveCamera;
